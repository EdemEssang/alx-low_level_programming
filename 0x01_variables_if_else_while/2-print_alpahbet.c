#include <stdio.h>

/**
 * A program that prints the alphabet in lowercase, followed by a new line
 * return 0.
 */
int main(void)
        {
	   char letter;
	      
	       for (letter = 'a'; letter <=; letter++)
		       putchar(letter);

	       putchar('\n');

	       return (0);

	}
